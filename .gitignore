*.db
*.db.*
secrets/
playwright-report/
test-results/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production
secrets.env
secrets-test.env
*.env.backup*
secrets*.backup*

# Backups (created by rotation scripts)
backups/

# Test reports
playwright-report/
dev-dist

# Lighthouse CI reports
.lighthouseci/

# Capacitor native builds (keep config and assets, exclude build artifacts)
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/.idea/
android/*.iml
ios/App/Pods/
ios/App/App.xcworkspace/xcuserdata/
ios/App/DerivedData/
ios/build/

# Generated PWA icons from capacitor-assets
icons/
coverage/

# lefthook per-repo overrides
lefthook-local.yml

# Playwright persisted auth sessions (real test-account sessions)
e2e/.auth/

# Local hosted-rowboat dev harness scratch state (scripts/dev-rowboat.sh)
.rowboat-dev/

# Hosted-rowboat tenant state (holds the once-shown management key) — visible, but never committed
rowboat-tenant.*.json

.env.tenant.local
